You can generate a random number between 1 and 100 in Python using the random module. Here is a simple example:

```python
import random
print(random.randint(1, 100))
```

This script will print a random number between 1 and 100 (inclusive) each time it is run.